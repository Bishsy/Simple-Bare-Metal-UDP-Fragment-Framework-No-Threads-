#include <stdint.h>
#include <string.h>

#define MAX_DATA        2000
#define MAX_FRAGS       32
#define FRAME_SIZE      600
#define TIMEOUT_TICKS   500

typedef struct {
    uint32_t msg_id;
    uint16_t frag_id;
    uint16_t total_frags;
    uint16_t size;
    uint8_t  acked;
} frag_info_t;
